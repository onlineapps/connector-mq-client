'use strict';

/**
 * Entry point for @onlineapps/agent-mq-client.
 * Exports the AgentMQClient class so that users can do:
 *    const AgentMQClient = require('@onlineapps/agent-mq-client');
 */

const AgentMQClient = require('./client');

module.exports = AgentMQClient;
